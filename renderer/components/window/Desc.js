import styled from 'styled-components'

const Desc = styled.p`
  line-height: 1.3;
  font-size: 16px;
  font-weight: normal;
  margin: 5px 0 10px 0;

  color: ${p => p.theme.colors.grayText};
`

export default Desc
